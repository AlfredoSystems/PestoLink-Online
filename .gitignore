node_modules/
dist/
.DS_Store
*.local
.env.local

# Capacitor Android platform (regenerated by `cap add android` + `cap sync android`)
android/
# ios/ is committed — native project template must be stable for signed App Store builds

# Electron build output and builder cache
electron-dist/
electron-builder-cache/

# Generated icon files (produced by scripts/generate-icons.js)
build/
